# Q11: Create a nested list containing a numeric vector and a character vector
df <- data.frame(Name = c("Alice", "Bob"), Age = c(25, 30))
nested_list <- list(Vector1 = c(1, 2, 3), Vector2 = c("A", "B", "C"))
print(nested_list)
